{% for year in archive_menu %}
<h2>{{ year }}</h2>
<div class="list-group">
    {% for month in archive_menu[year] %}
        <a href="{{ archive_menu[year][month] }}" class="list-group-item list-group-item-action">Telco Oběžník {{ month }}</a>
    {% endfor %}
</div>
{% endfor %}
